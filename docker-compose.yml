version: "3"

services:
  jupyter:
    container_name: metview-jupyter
    build:
      context: .
    ports:
      - 8888:8888
    environment:
      JUPYTER_TOKEN: ${JUPYTER_TOKEN}
    volumes:
      - ./notebooks:/root/notebooks:rw
      - ./sample-data:/root/sample-data:ro